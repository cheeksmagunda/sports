[project]
name = "nhl-oracle"
version = "0.1.0"
description = "NHL application scaffold for Sports Oracle."
readme = "README.md"
requires-python = ">=3.11,<3.13"
license = { text = "Proprietary" }
authors = [{ name = "Ben Oracle" }]

dependencies = [
    "oracle-core==0.1.0",
    "httpx>=0.28,<0.30",
    "playwright>=1.59.0",
]

[tool.uv.sources]
oracle-core = { workspace = true }

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3,<10.0",
    "pytest-asyncio>=0.24,<2.0",
    "ruff>=0.7,<1.0",
    "mypy>=2.3.1,<3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nhl_oracle"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["nhl_oracle"]
mypy_path = ["src"]

[project.scripts]
nhl-live-contract-audit = "nhl_oracle.ingest.audit:main"
