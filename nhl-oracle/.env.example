# NHL Oracle environment template.
# Keep secrets in process environment, Codespaces secrets, or encrypted local files.
# No runtime variable is read yet; declare each name here when code first reads it.
