// Track record: 30-slate outcome strip + recent-slates table. Built in
// Phase 5; this stub only proves the route resolves and nav reaches it.

import { Shell } from "../components/Shell";

export function HistoryPage() {
  return (
    <Shell>
      <main className="stub-page" aria-label="Track record">
        <h1>Track record</h1>
        <p>Recent slates and outcomes are coming in Phase 5.</p>
      </main>
    </Shell>
  );
}
