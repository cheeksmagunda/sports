# data/ changelog

Daily artifacts written to `data/processed/` and `data/raw/` are
git-ignored. Reference artifacts that ship in the image (small lookup
tables, identity overrides, fixture replays) are committed here.

Empty until Step 3 ingest writes the first reference table.
