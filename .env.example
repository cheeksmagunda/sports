# Optional root-common HTTP automation capability. Native gh and Railway CLI
# sessions stay in their native credential stores and are not duplicated here.
# auth:optional
RAILWAY_WORKSPACE_TOKEN=

# Optional local SOPS identity path. The private identity stays outside this
# repository with mode 0600. SOPS may use its standard age identity location.
SOPS_AGE_KEY_FILE=
